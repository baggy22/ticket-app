Packages needed for install:

python-flask

you need to install pip which is a python packet manager 

instructions here -https://pip.pypa.io/en/latest/installing.html

if using pip

install falks should be 

$pip install flask
(might need to use sudo command)

note from sid regarding trouble shooting:

pip install -U Flask-WTF

There are other dependencies listed here

http://blog.miguelgrinberg.com/post/the-flask-mega-tutorial-part-i-hello-world

they are
$ pip install flask-login
$ pip install flask-openid
$ pip install flask-mail
$ pip install flask-sqlalchemy
$ pip install sqlalchemy-migrate
$ pip install flask-whooshalchemy
$ pip install flask-wtf
$ pip install flask-babel
$ pip install guess_language
$ pip install flipflop
$ pip install coverage

http://www.fullstackpython.com/flask.html

Should be easy enough to install, dont think any other packges are needed.

Might need to run this too: pip install -U Flask-WTF

Instructions to Run:

python run.py - terminal shows stats

in the webbrowser: http://127.0.0.1:5000/
